def main with input x output y as
    if x < of_bool false then
        y := 0
    else (
        y := 1;
        while (not (x < 1)) do (
            y := y * x;
            x := x - 1
        )
    );
    skip