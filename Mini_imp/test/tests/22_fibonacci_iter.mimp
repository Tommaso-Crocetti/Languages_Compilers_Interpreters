def main with input x output y as
    if x < 1 then
        y := 0
    else (
        a := 0;
        b := 1;
        while 1 < x do (
            t := a + b;
            a := b;
            b := t;
            x := x - 1
        );
        y := b
    )
