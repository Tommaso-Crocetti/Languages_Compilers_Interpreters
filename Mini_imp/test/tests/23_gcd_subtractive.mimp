def main with input x output y as
    a := x;
    if a < 0 then a := 0 - a else skip;
    if a < 1 then a := 0 else skip;
    b := a + 1;
    while (a < b) or (b < a) do
        if a < b then b := b - a
        else a := a - b;
    y := a
