def main with input x output y as
    a := x;
    if a < 0 then a := 0 else skip;
    y := 0;
    i := a;
    while 0 < i do (
        j := a - 1;
        while 0 < j do (
            y := y + 1;
            j := j - 1
        );
        i := i - 1
    )